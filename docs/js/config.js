/**
 * ParlaBio – Konfiguration.
 * URLs und Konstanten zentral ändern statt im Code suchen.
 */
const CONFIG = Object.freeze({
  SEARCH_INDEX_URL: 'data/search-index.json',
  PERSON_DETAIL_URL: 'data/person/',
  PAGE_SIZE: 50,
  PAGINATION_RANGE: 2,
  TOP_FACTIONS_COUNT: 8,
});
